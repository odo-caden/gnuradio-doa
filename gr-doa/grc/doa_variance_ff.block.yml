# auto-generated by grc.converter

id: doa_variance_ff
label: Variance
category: '[doa]'

parameters:
-   id: vec_len
    label: Vector Length
    dtype: int
    default: '1'

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vec_len }

outputs:
-   domain: stream
    dtype: float

templates:
    imports: import doa
    make: doa.variance_ff(${vec_len})

file_format: 1
